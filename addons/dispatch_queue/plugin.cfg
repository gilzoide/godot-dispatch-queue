[plugin]

name="Dispatch Queue"
description="Dispatch Queue object implementation.

Supports synchronous and threaded execution, with the later being either serial (1 Thread) or concurrent (2+ Threads).
If threading is not supported by OS, fallbacks to running in synchronous mode."
author="gilzoide"
version="0.1.0"
script="plugin.gd"
